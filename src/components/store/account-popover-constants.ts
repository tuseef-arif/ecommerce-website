/** DOM id for the dialog — keep in sync with `StoreHeader` aria-controls / outside-click checks */
export const ACCOUNT_POPOVER_ELEMENT_ID = "header-account-popover";

/** Success sheet portaled above the account dialog — `StoreHeader` must treat as in-dialog for Escape / outside-click */
export const STORE_AUTH_SUCCESS_ELEMENT_ID = "store-auth-success-overlay";
